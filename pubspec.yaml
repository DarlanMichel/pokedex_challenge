name: challenge_pokedex
description: A project challenge for snapfi.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  http: ^0.13.6
  dartz: ^0.10.1
  get_it: ^7.6.0
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  bloc: ^8.1.2
  bloc_concurrency: ^0.2.2
  stream_transform: ^2.1.0
  custom_pop_up_menu: ^1.2.4
  google_fonts: ^4.0.4
  cached_network_image: ^3.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
  bloc_test: ^9.1.2
  mocktail: ^0.3.0

flutter:

  uses-material-design: true

  assets:
    - assets/pokeball.png
    - assets/straighten.png
    - assets/weight.png

  